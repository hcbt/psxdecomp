[project]
name = "psx-decomp"
version = "0.0.0"
description = "PS1 matching decompilation"
requires-python = ">=3.11"
dependencies = [
    "splat64[mips]>=0.50.0,<1.0.0",
]

[tool.uv]
package = false

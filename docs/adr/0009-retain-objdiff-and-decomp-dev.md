# Retain objdiff and decomp.dev

Objdiff remains the function-comparison engine and its report remains compatible with decomp.dev. Meson supplies fresh, structurally namespaced objects from its explicit graph, eliminating stale filesystem discovery and cross-binary symbol collisions without replacing the established comparison and publishing tools.

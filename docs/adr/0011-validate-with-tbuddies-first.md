# Validate with tbuddies first

The toolkit and project manifest remain game-agnostic, but the first implementation is validated against `tbuddies-decomp`. Additional abstractions wait until another game project demonstrates a concrete variation, avoiding speculative compatibility while preserving the reusable-toolkit boundary.

# Provide a reusable toolkit

`psxdecomp` is a reusable toolkit consumed by game projects rather than a copied template or a game-specific implementation. The toolkit owns shared reconstruction and verification capabilities, while each game project owns its game-specific inputs and declares them explicitly.
